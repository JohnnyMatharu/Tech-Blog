<article class="post">
  <div class="title">
    <a target="_blank">{{title}}</a>
  </div>
  <div class="meta">
  by {{user.username}} on {{format_date}}
    |
    <a href="/post/{{id}}">{{comments.length}}</a>
  </div>
</article>